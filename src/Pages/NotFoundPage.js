import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "90px",
        color: "#fff",
      }}
    >
      Not Found
    </div>
  );
};

export default NotFoundPage;
